<nav>
    <!-- ...existing navigation items... -->
    <% if (!user.subscription || user.subscription.status !== 'active') { %>
        <a href="/subscription/plans" class="upgrade-button">Upgrade to Pro</a>
    <% } %>
</nav>
